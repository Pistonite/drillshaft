[package]
name = "shaft-registry-build"
description = "build script for the shaft registry"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
cu = { workspace = true, features = ["fs", "json", "derive"] }
pm = { workspace = true, features = ["full"] }
itertools.workspace = true
